import React, { useState, useEffect } from 'react';
import { BrowserRouter as Router, Route, Routes, Link } from 'react-router-dom';

const Shows: React.FC = () => {
    return (
      <div>
        <h1>Shows</h1>
        <ul>
          Shows Here use fetch and hooks
        </ul>
      </div>
    );
  };
  
  export default Shows;